# Crypto Cycle Strategy Arena — Python dependencies
# Pinned with lower bounds (no lockfile yet); the locked tech stack is in the plan.

# --- Web layer (Stage 0+) ---
fastapi>=0.115
uvicorn[standard]>=0.30

# --- Data layer (Stage 1+) ---
# Included now so the Docker image is Stage-1 ready and we avoid a rebuild churn.
pandas>=2.2
numpy>=1.26
requests>=2.32
yfinance>=0.2.40

# --- Tests (Stage 2+) ---
pytest>=8.0
httpx>=0.27        # FastAPI TestClient transport (Stage-4 API tests)
